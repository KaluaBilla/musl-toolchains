# musl-toolchains

These toolchains are downloaded from [musl.cc](https://musl.cc) (all credit to them).  
They are hosted here to bypass network restrictions in GitHub Actions, so workflows can use them without direct access to musl.cc.

## Usage

Download and extract the toolchain in your workflow, then add its `bin` directory to `PATH`.
